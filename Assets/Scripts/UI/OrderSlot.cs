using UnityEngine;
using UnityEngine.UI;

public class OrderSlot : MonoBehaviour
{
    Image icon;
    ItemManager item;
    // Start is called before the first frame update
    void Start()
    {
      
    }
    
    public void addItem(ItemManager newItem)
    {
        item = newItem;

       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
